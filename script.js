const nav=document.querySelector('.nav');const reveals=[...document.querySelectorAll('.reveal')];const reduced=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}})},{threshold:.12});reveals.forEach((el,i)=>{el.style.transitionDelay=`${Math.min((i%4)*70,210)}ms`;observer.observe(el)});window.addEventListener('scroll',()=>nav.classList.toggle('scrolled',window.scrollY>30),{passive:true});if(!reduced){const hero=document.querySelector('.hero-media');const rating=document.querySelector('.rating-badge');const galleryPhotos=[...document.querySelectorAll('.gallery-photo')];let ticking=false;const animate=()=>{const y=window.scrollY;if(hero&&y<window.innerHeight*1.2){hero.style.transform=`scale(${1.04+y*.00009}) translateY(${y*.035}px)`;if(rating)rating.style.transform=`translateY(${-y*.025}px)`}galleryPhotos.forEach(photo=>{const rect=photo.parentElement.getBoundingClientRect();if(rect.top<window.innerHeight&&rect.bottom>0){const progress=(window.innerHeight-rect.top)/(window.innerHeight+rect.height);photo.style.transform=`scale(1.06) translateY(${(progress-.5)*18}px)`}});ticking=false};window.addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(animate);ticking=true}},{passive:true});document.querySelectorAll('.food-card').forEach(card=>{card.addEventListener('mousemove',e=>{const r=card.getBoundingClientRect();const x=(e.clientX-r.left)/r.width-.5;const y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateX(${-y*2.5}deg) rotateY(${x*2.5}deg)`});card.addEventListener('mouseleave',()=>card.style.transform='')})}