const nav=document.querySelector('.nav');const reveals=document.querySelectorAll('.reveal');const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}})},{threshold:.12});reveals.forEach(el=>observer.observe(el));window.addEventListener('scroll',()=>nav.classList.toggle('scrolled',window.scrollY>30),{passive:true});if(!window.matchMedia('(prefers-reduced-motion: reduce)').matches){window.addEventListener('scroll',()=>{const y=window.scrollY;const media=document.querySelector('.hero-media');const coffee=document.querySelector('.coffee');if(y<window.innerHeight){media.style.transform=`scale(${1+y*.00008}) translateY(${y*.05}px)`;coffee.style.marginBottom=`${y*.04}px`}}, {passive:true})}